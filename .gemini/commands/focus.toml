description = "Sur quoi travailler là, maintenant ?"
prompt = """
Lis dans cet ordre :
1. !{bash -c 'source scripts/config.env; cat "$DESPES_NOTES/Polaris.md"'} (boussole)
2. !{bash -c 'source scripts/config.env; cat "$GLOBAL_DIR/index.md"'} (état tous projets)
3. !{bash -c 'source scripts/config.env; ls -t "$DESPES_NOTES/_daily/" | head -3 | while read f; do echo "=== $f ==="; cat "$DESPES_NOTES/_daily/$f"; done'} (énergie / mode)
4. @{memory.md} (projet actif)

Recommande exactement :
- **1 action principale** — la plus utile maintenant
- **Pourquoi** — cohérence avec le Life Razor et le Top of Mind
- **Ce à éviter** — ce qui serait une distraction selon Polaris

⚠️ Ne touche à aucun fichier.
"""
